## Project knowledge

This repository contains a Grafana panel plugin. Read `./.config/AGENTS/instructions.md` before changes.

Treat `docs/data-model.md` and `src/types.ts` as public contracts. Keep option migrations backward-compatible, preserve the plugin ID, and update contract documentation and tests with user-visible behavior. Never modify `.config/` directly.
